{
  "name": "@bizyaml/cli",
  "version": "1.0.0",
  "description": "Command Line Interface for BizYAML",
  "bin": {
    "bizyaml": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rimraf dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@bizyaml/parser": "*",
    "cac": "^6.7.14"
  },
  "devDependencies": {
    "vitest": "^1.6.1"
  }
}
